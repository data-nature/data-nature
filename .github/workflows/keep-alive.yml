name: Keep Streamlit Awake
on:
  schedule:
    - cron: "0 */6 * * *"   # every 6 hours
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the app
        run: curl -s -o /dev/null -L "https://<your-app>.streamlit.app/"
